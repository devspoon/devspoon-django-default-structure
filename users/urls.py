from django.urls import path, include
from . import views

# from .views import RegisterView, VerificationEmailView, VerificationSMSView, LoginView, LogoutView, ProfileView, PasswordView

urlpatterns = [
    # path('register/', RegisterView.as_view(), name='register'),
    # path('verify/email/', VerificationEmailView.as_view(), name='verification'),
    # path('verify/sms/', VerificationSMSView.as_view(), name='verification'),
    # path('login/', LoginView.as_view(), name='login'),
    # path('logout/', LogoutView.as_view(), name='logout'),
    # path('profile/', ProfileView.as_view(), name='profile'),
    # path('password/', PasswordView.as_view(), name='password'),
]